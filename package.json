{
  "name": "card-mover-backend",
  "version": "1.0.0",
  "description": "Demo project using React + Express + Socket.io application.",
  "main": "server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "build": "cd client && npm run build",
    "postinstall": "cd client && npm install"
  },
  "type": "module",
  "keywords": [],
  "author": "Giliardi Schmidt",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "socket.io": "4.1.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  },
  "nodemonConfig": {
    "ignore": [
      "client/"
    ]
  }
}
